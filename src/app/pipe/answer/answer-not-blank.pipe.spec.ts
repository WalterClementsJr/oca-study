import {AnswerNotBlankPipe} from './answer-not-blank.pipe';

describe('AnswerNotBlankPipe', () => {
  it('create an instance', () => {
    const pipe = new AnswerNotBlankPipe();
    expect(pipe).toBeTruthy();
  });
});
